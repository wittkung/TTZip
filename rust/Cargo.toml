[workspace]
members = [
    "ttzip-glue",
    "ttzip-tui",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

[profile.test]
opt-level = 3
debug = 1

